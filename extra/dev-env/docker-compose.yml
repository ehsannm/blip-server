version: '2'
services:

  # API Server
  edge:
    restart: always
    image: registry.ronaksoftware.com/river/server/server-api-edge:dev
#    environment:
    networks:
      - blip

networks:
  blip:
    external:
      name: blip
